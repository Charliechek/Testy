<!DOCTYPE html>
<html lang="cs">

    <head>
        <meta charset="UTF-8">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
        <title>{% block title %}{% endblock %}</title>
    </head>

    <body>

        <nav class="navbar navbar-expand-lg bg-light mb-5">
            <div class="container-fluid">

                <div class="collapse navbar-collapse justify-content-start">
                    <ul class="navbar-nav mb-2 ms-5">
                        <li class="nav-item px-5">
                            <a class="nav-link" href="{{ path('testy_index') }}">Testy</a>
                        </li>
                    </ul>
                </div>

                <div class="collapse navbar-collapse justify-content-end">
                    <ul class="navbar-nav mb-2 me-5">

                        {% if app.user %}
                            <li class="nav-item px-5 d-flex">                        
                                <span class="pt-2">Jste přihlášeni jako {{ app.user.userIdentifier }} </span>
                                <a class="nav-link" href="{{ path('uzivatel_odhlaseni') }}">Odhlásit</a>
                            </li>
                        {% else %}
                            <li class="nav-item px-5">
                                <a class="nav-link" href="{{ path('uzivatel_prihlaseni') }}">Přihlášení</a>
                            </li>
                            <li class="nav-item px-5">
                                <a class="nav-link" href="{{ path('uzivatel_registrace') }}">Registrace</a>
                            </li>
                        {% endif %}

                    </ul>
                </div>
                
            </div>
        </nav>

        {% block body %}{% endblock %}
        
    </body>
    
</html>
